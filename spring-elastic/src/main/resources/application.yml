# 服务配置
server:
  port: 8000

# spring 配置
spring:
  application:
    name: elasticsearch
  datasource:
    #    url: jdbc:mysql://47.105.218.58:3306/qq?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://localhost:3306/nacos?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
  output:
    ansi:
      enabled: always
#logging:
#  level:
#    org.springframework.data.elasticsearch: debug
