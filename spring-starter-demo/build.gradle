dependencies {
    compile(project(":spring-starter"))

    compile(
            "io.springfox:springfox-swagger2:${swaggerVersion}",
            "com.github.onsoul:springfox-swagger-ui:${swaggerGitHubUi}"
    )
}
